项目名称；
    计算器
功能：
    进行简单的数学运算，支持的运算符号包括：+、-、*、/、(、)
运行：
    python main.py  # 提示输入数学公式，然后回车进行计算
实现：
    使用正则表达式来匹配每一个数字和运算符字符，进行分组、排列，
    通过计算优先级来确定正确的计算顺序，
    利用堆栈先进后出的思想，将公式按顺序计算
注意事项：
    如果输入的公式中存在数字、[+、-、*、/、(、)]之外的值，则会直接返回False
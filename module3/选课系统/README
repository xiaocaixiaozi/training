项目名称：
    选课系统
功能：
    提供3个接口：
        讲师：上课、查看学员、批改作业
        学员：购买课程、查看课程、上课打卡、提交作业、查询成绩
        管理员：创建校区、课程、班级、指定讲师
    将创建的对应关系全部序列化到文件中
实现：
    创建三个类，继承关系如下：
        School > Student > Teacher
        Teacher类维护上课记录的数据
        Student类维护学习记录的数据
        课程、班级、班级与讲师关系、班级与学员关系、课程与学员关系、课程与校区关系、讲师与校区关系、学员与校区
            -- 关系都由School类维护
    将创建好的对应关系使用pickle序列化到文件中，下次启动时，会去读取配置
运行；
    python main.py
    入口为三个：讲师、学员、管理员；学员有注册和登陆两个接口。
注意：
    修改完成后，需要正常退出程序，否则会导致数据不能序列化成功，下次启动时数据异常。
现有数据：
    Shanghai校区：
        课程：Go
        班级：go
        讲师：teacher01, teacher02 [全部负责带go班级]
        学员：student02，购买Go课程
    Beijing校区：
        课程：Python、Linux
        班级：python、linux
        讲师：teacher03、teacher04 --> 带python班的课
             teacher05 --> 带linux班的课
        学员：student01，购买Python课程
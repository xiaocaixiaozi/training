1、A = '0.1'
    int(A)  # 不能使用int转换A
        ValueError: invalid literal for int() with base 10: '0.1'
    float(A)    # 使用float来转换
2、粘包：多次发送的数据，被对端一次性接受，出现解析错误
    解决方法：在接收一次数据后，接收端可以发送一次数据，用来阻塞，等待发送端接受后，再次发送剩余数据

3、断点续传：通过已传输的字节数除以总字节数，将float转换为int，然后乘以要显示的长度，
    print结尾为'\r'，即为进度条，例如：
        trans_percent = int(50 * (filecount / filesize))
        print(('[%s>' % (trans_percent * '=')).ljust(50) + ']', end='\r')

1、项目名称：
    切换VPN账号

2、目的：
    此脚本用来实现切换正式网和测试网VPN的功能，不用每次都去修改地址、用户名、密码等，只需在第一次执行之前修改好就可以。

3、执行：
    解压缩包到sslvpn-client.exe程序存在的目录，进入目录，执行 switch_vpn.exe 即可

4、提示：
    如果首次运行，会在上级目录生成一个默认名为"vpn.ini"的配置文件，需要将它的内容修改为正确的值，初始内容如下：
    [offical_vpn]           # 大网VPN
    ip_addr = ip            # 服务器IP地址
    port = port             # 端口
    auto_login = 0          # 是否自动登录，0为否，1为是
    user_name = bloke       # 用户名
    password = bloke_pass   # 密码，此密码非账号密码，需要从源配置文件中将加密后的密码串拷贝过来

    [lab_vpn]               # 测试网VPN
    ip_addr = ip            # 服务器IP地址
    port = port             # 端口
    auto_login = 0          # 是否自动登录，0为否，1为是
    user_name = bloke       # 用户名
    password = bloke_pass   # 密码，此密码非账号密码，需要从源配置文件中将加密后的密码串拷贝过来

    * 密码为加密后的字符串，因不知道源加密方式，所以只能通过拷贝的方式来填写，从源配置文件中将密码串拷贝过来；
    * 源配置文件为上级目录中的"sslvpn.ini"
    * 必须配置正确，之后脚本会读取此配置文件

5、描述：
    脚本大致分为4步：
        读取配置文件
        将修改写入源配置文件
        停止进程
        启动进程

6、注意：
    如果执行失败，不可再次执行，因为服务器端可能会通过尝试错误次数来锁定账号

